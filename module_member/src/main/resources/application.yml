spring:
  application:
    name: member
    
  profiles:
    include: redis,feign #redis모듈에 있는 application-redis.yml을 같이 읽어옴

  datasource:
    # MySQL
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/hospital_visit_service?&serverTimezone=Asia/Seoul
    username: root
    password: 385tjdgus
#    url: ${MYSQL_URL}
#    username: ${MYSQL_USERNAME}
#    password: ${MYSQL_PASSWORD}

  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        database-platform: org.hibernate.dialect.MySQL8Dialect
    hibernate:
#      ddl-auto: create-drop #애플리케이션 시작 시 기존 스키마 삭제 후 생성,애플리케이션 종료 시 스키마 삭제
      ddl-auto: create # 애플리케이션 시작 시 기존 스키마 삭제 후 생성, 애플리케이션 종료 시 스키마 삭제 X(데이터 남아있음)
      database-platform: org.hibernate.dialect.MySQL8Dialect

#  data:
#    redis:
#      host: localhost
#      port: 6379
#      password: 385tjdgus
#      host: ${REDIS_HOST}
#      port: ${REDIS_PORT}
#      password: ${REDIS_PASSWORD}

#redis:
#  keys:
#    pending-id-prefix: "member:pending:id:"

#logging:
#  level:
#    org.springframework.cache: trace # Redis 사용에 대한 로그가 조회되도록 설정
#    org.springframework.data.redis: debug # Redis 명령 실행 로그

member:
  valid-id:
    timeout: 300 #5분
    prefix: "member:pending:id:"